.flex.flex-col.text-zinc-500.disabled:text-zinc-300.disabled:cursor-not-allowed.sm:text-sm.text-xs{id: "cart-#{@cart.id}-dropdown-variant-pair", data: {"carts--item-target": "dropdown", action: "dropdown#toggle click@window->dropdown#hide"}, disabled: cannot?(:update, @cart)}
  .sm:flex.gap-2.items-center.hidden
    Variation:
  = @variant_pair
  = render StockCountComponent.new count: @count_on_hand, sale: @sale
